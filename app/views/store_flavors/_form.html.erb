<%= form_for(@store_flavor) do |f| %>

  <% if @store_flavor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@store_flavor.errors.count, "error") %> prohibited this store_flavor from being saved:</h2>

      <ul>
      <% @store_flavor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
	<div class="col s8 centered">
	<table border="0" cellspacing="5" cellpadding="5">
		<tr valign="top">
			<td>
  <div class="input-field">
			    <%= f.label :store_id %><br />
			    <%= f.collection_select :store_id, Store.alphabetical, :id, :name, :prompt => "Select ..." %>
			  </div>
			  
		<div class="input-field">
			    <%= f.label :flavor_id %><br />
			    <%= f.collection_select :flavor_id, Flavor.alphabetical, :id, :name, :prompt => "Select ..." %>
			  </div>

			  <div class="actions">    
			  	<%= f.submit :class=>"btn waves-effect waves-light btn green" %>     
			  	<%= link_to 'Cancel', store_flavors_path, :class => 'waves-effect waves-light btn red' %>   
			  </div>
			</td>
			<td width="25">&nbsp;</td>
		</tr>
	</table>
	</div>
 <% end %>
